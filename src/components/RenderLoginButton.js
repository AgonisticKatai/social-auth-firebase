import React, { Component } from "react";

class RenderLoginButton extends Component {
  constructor(props) {
    super(props);
    this.state = {
    };
  }

  render() {
    return (
      <h1>Component .env</h1>
    )
  }
}

export default RenderLoginButton;
